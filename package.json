{
  "devDependencies": {
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
    "@types/jest": "^29.4.0",
    "axios": "^1.3.4",
    "cypress": "^12.7.0",
    "cypress-xpath": "^2.0.1",
    "esbuild": "^0.17.10",
    "jest": "^29.4.3",
    "ts-jest": "^29.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "jest-unit-tests": "npx jest --testMatch='**/tests/*.unit.spec.ts'",
    "jest-api-tests": "npx jest --testMatch='**/tests/*.api.spec.ts'",
    "cypress-e2e-tests": "npx cypress run",
    "run-all-tests": "npx jest && npx cypress run"
  }
}
